import { ChangeThemeColorDirective } from './change-theme-color.directive';

describe('ChangeThemeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeThemeColorDirective();
    expect(directive).toBeTruthy();
  });
});
