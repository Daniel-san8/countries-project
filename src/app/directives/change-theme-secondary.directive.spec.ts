import { ChangeThemeSecondaryDirective } from './change-theme-secondary.directive';

describe('ChangeThemeSecondaryDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeThemeSecondaryDirective();
    expect(directive).toBeTruthy();
  });
});
